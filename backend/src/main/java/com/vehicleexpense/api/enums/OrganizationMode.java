package com.vehicleexpense.api.enums;

/**
 * Organization Mode Enum - Matches PostgreSQL organization_mode type
 * SOLO: Individual user (1-person organization)
 * FLEET: Multiple users with fleet management
 */
public enum OrganizationMode {
    SOLO,
    FLEET
}
